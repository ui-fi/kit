{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "lint": "lerna run lint",
    "test": "lerna run test",
    "publish:prepatch:beta": "lerna publish --preid beta prepatch",
    "publish:prepatch:canary": "lerna publish --preid beta --canary prepatch --npm-client npm",
    "publish:canary": "lerna publish --canary",
    "showcase:basic": "cd ./basic/ && yarn showcase"
  },
  "devDependencies": {
    "grunt": "^1.0.3",
    "grunt-ts": "^6.0.0-beta.21",
    "istanbul": "^0.4.5",
    "lerna": "^3.3.0",
    "nyc": "^13.1.0"
  },
  "dependencies": {
    "tslint": "^5.11.0",
    "typescript": "^3.1.3"
  }
}
